# 小乔点击工具 WPF 版测试记录

测试日期：2026-05-24

## 自动验证

- `dotnet build -c Release`：通过，0 error / 0 warning
- 发布版启动烟测：通过，启动后 3 秒内未退出
- 安装包生成：通过，输出 `安装包\小乔点击工具 1.1.7.exe`
- 安装脚本检查：通过，`[Run]` 未配置自动启动程序
- 静默安装验证：通过，安装到 `%LocalAppData%\Programs\小乔点击工具`
- 安装完成不自动启动验证：通过，未发现 `XiaoQiaoClickTool` 进程
- 静默卸载验证：通过，主程序文件已清理
- 项目清理验证：通过，旧 Python/WebView 文件、旧安装包、临时构建缓存已清理
- Logo 资源验证：通过，`logo.png` 已改为嵌入资源并通过 pack URI 加载
- 全局快捷键构建验证：通过，F6 开始/继续、F7 暂停、F8 停止已注册
- 正式 `.ico` 图标：通过，已由根目录 `logo图标.png` 重新生成并接入 exe / 安装包
- 范围切换构建验证：通过，圆形范围 / 矩形范围使用同一分组并支持反复切换
- 范围数值输入构建验证：通过，圆形范围 / 矩形范围支持手动输入 10-160
- 暂停/继续构建验证：通过，长间隔等待拆分为小步，暂停/继续可即时响应
- 停止初始化构建验证：通过，手动停止会记录历史后回到初始状态
- 按钮状态构建验证：通过，暂停/继续运行中高亮可用，停止按钮默认显示“已停止”
- 范围默认值构建验证：通过，圆形范围 / 矩形范围默认 17，输入框支持键盘上下键微调
- 取点 Overlay DPI 构建验证：通过，Overlay 使用鼠标所在屏幕 DPI 和物理像素定位，减少不同缩放电脑上的圆点偏移
- 点击规则/停止方式切换构建验证：通过，固定/随机、时间/次数/手动均使用统一分组并强制互斥
- 完成提示：已接入系统提示音和确认框，确认后初始化
- 历史记录：已接入 `%AppData%\XiaoQiaoClickTool\history.json`，最多保留 10 条
- 点击计数可靠性验证：通过，只有 `SetCursorPos` 和 `SendInput` 成功后才累计成功点击；失败最多重试 3 次，失败不计数但累计发送次数并继续任务
- 连续失败保护构建验证：通过，连续失败 30 次后红色状态提示并持续写日志，任务不停止
- 失败率提示构建验证：通过，发送次数达到 20 且失败率超过 10% 时显示红色提示
- 桌面图标盾牌验证：通过，程序 manifest 改为 `asInvoker`，默认不请求管理员权限
- 防休眠提醒构建验证：通过，次数任务达到 1000 次及以上启动前弹出提醒
- 停止去重验证：通过，连续停止/快捷键停止不会重复写入历史

## 待人工验收

- 打开软件 3 分钟不未响应
- 连续拖动圆形滑块 20 次不卡死
- 连续拖动矩形滑块 20 次不卡死
- 来回切换圆形/矩形 20 次不卡死
- 点击选择点，移动鼠标时范围预览正常显示
- 取点完成后主界面不未响应，并更新坐标
- 固定时间 1 秒运行 30 次点击，不未响应
- 随机时间运行 30 次点击，不未响应
- 运行中修改停止方式，不未响应
- 点击停止后 1 秒内停止
- 安装完成后不会自动弹出软件遮挡安装器完成页：自动验证通过
- 卸载后主要安装文件被清理：自动验证通过

## 备注

当前安装包是 self-contained 发布，优点是目标电脑不需要单独安装 .NET 运行时，缺点是安装包体积较大。

代码签名未执行：当前机器没有可信代码签名证书。若要消除 Windows “未知发布者”提示，需要购买/配置正式代码签名证书后再签名安装包。
